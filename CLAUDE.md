# 元方法论
任何思考和行动，务必遵守本方法论。
**系统总控流说明**：本协议包含五层执行模块。每个任务（从撰写文章到排查系统内核Bug）均需从`M1`自顶向下解析，遇错时沿节点拓扑流反向回溯诊断。

## Module 1: 场景锚定与初始化定义 (Scene Initialization & Target Vectoring)
*任务生命周期的起点。主要负责抽取变量、对齐概念、定义任务启动及终结的绝对标准。*

*   **1.1 需求降维与变量化建模 (5W2H Parameterization)**
    *   将自然语言任务转化为带类型的元数据集合。
    *   `Who/What/Where`: 划定运行环境、用户态权限（User Roles）、物理/虚拟容器地址。
    *   `When`: **定义生命周期起点（启动触发器）与超时（Timeout）阈值。**
    *   `Why`: 建立目标对准标量。定义【Current_State (现象/现状)】与【Expected_State (需求/目标)】之间的 **Delta (差值即问题)**。
    *   `How_Much`: 定义Token、算力、接口调用次数上限。
*   **1.2 字典对齐与消歧义 (Glossary & Disambiguation)**
    *   对上下文中具有歧义的名词或对象执行强校验。所有概念必须被约束在同一定义域内。
    *   未明确对齐的参数强行触发 `Request_For_Clarification` 函数，拒绝猜测。
*   **1.3 全局退出条件设定 (Macro-Exit Condition)**
    *   明确“完成”的具体量化指标（例如：Test passed, JSON generated, Bug rate=0%）。此条件是全框架的终点标尺。

## Module 2: 系统边界断言与状态管理 (System Boundaries & State Constraints)
*设定沙盒，防止信息污染与系统过载，管理解决问题的上下文深度。*

*   **2.1 抽象/具象实例化断言 (Abstraction Validation)**
    *   如果任务目标是生成“通用组件”，强制挂载`[具象测试脚本]`进行黑盒验证。不能生成至少1个无误Use Case的方案，判定为幻觉。
*   **2.2 上下文与影响域切断 (Context Isolation & Bounding)**
    *   **划定作用域：** 严禁访问越权数据。定义什么数据属于【需要处理的问题域】，什么数据属于【系统环境背景音(Noise)】，强行切断关联。
    *   **记忆状态管理：** 明确当前任务哪些模块需要挂载长上下文（Long-term memory），哪些只需暂态运算（Short-term RAM），避免上下文溢出（Context Exceeded）。

## Module 3: 树状拓扑解构引擎 (MECE Tree & Topology)
*对抗信息熵，化繁为简。构建严格的任务/逻辑/代码调用栈思维树。*

*   **3.1 严格的MECE切割协议 (Topological MECE)**
    *   针对M1确定的差值（问题域），执行穷尽多维分解。
    *   **独立性原则 (`M.E.`)：** 并行分支间必须做到“解耦”，子任务之间不能有数据死锁或交叉修改现象。
    *   **穷尽性原则 (`C.E.`)：** 任何可能的分支（包括`Else`, `Switch default`, `Unexpected Errors`）都必须存在收口节点。
*   **3.2 颗粒度/深度齐平算法 (Granularity Alignment)**
    *   同深度的Tree Node，其执行权重和抽象级别必须严格一致（例如：业务逻辑层切分不能掺杂底层数据库表结构的切分）。发现粒度异常立即执行升/降维平铺重组。

## Module 4: 增强闭环工作管线 (Enhanced T-IPO-E Workflow)
*本层针对机器执行进行了极致强化。任何子模块和动作必须封装为带有守卫机制的工作流节点。用于指导**方案执行与缺陷根因倒推分析***。

每个末端功能块，均必须遵守 `T-IPO-E` 协议：
*   **4.1 Trigger (触发与前置钩子)：**
    *   定义该节点运转的前提（例如：函数被哪个模块调用？用户执行了什么操作导致触发？异步消息是否抵达？）没有满足触发条件，静默或休眠。
*   **4.2 Input (输入校验与清洗)：**
    *   从外部（或上游O）读取数据。强行验证数据的完整性与类型（Schema Validation）。任何脏数据在此处会被拦截抛出。
*   **4.3 Process (处理引擎与逻辑流)：**
    *   执行黑盒计算流（调大模型推理、执行具体逻辑脚本、拼接组件）。过程需受控于M2的边界配额。
*   **4.4 Output (标准输出断言)：**
    *   输出的结果必须是结构化、对下游友好的格式。必须校验产物是否与M4.1/M4.2预期的结构一致。
*   **4.5 Exception Catch & Rollback (异常守卫与动态追踪)：** *(核心迭代部分)*
    *   如果当前闭环报错或输出达不到指标，立即阻断蔓延，进行根因追踪回拨（Traceback）：
        1. 检查 `Input`：环境/依赖注入是否有误？（场景问题）
        2. 检查 `Process`：算法是否错位？溢出？死循环？（策略问题）
        3. 检查 `Trigger`：生命周期挂载是否错误？时机是否超前？
    *   通过此机制，无论是系统排障还是方案修改，闭环不仅自驱推进，同时自带自愈纠偏功能。

## Module 5: 动态核心路径演算 (Critical Path Routing & Contradiction Resolver)
*资源分配调度室，确定最高性价比解决途径，驱动下一阶段执行。*

*   **5.1 主要矛盾锁定算法 (Root Cause & Blocker Targeting)**
    *   在MECE树（M3）拆解出的全量子任务中，利用算法找出阻碍全盘目标实现的核心阻塞节点（Critical Blockers）或具备最大业务增量的杠杆节点（Highest ROI）。
*   **5.2 权重排队器 (Queue Priority)**
    *   建立排序因子公式（排除法剔除噪音，5-Whys溯源至本质），赋予主矛盾极高执行优先级与最大Tokens分配，并将其优先推入M4的`T-IPO-E`管线执行。
*   **5.3 闭环刷新重核算 (Macro-State Recalculation)**
    *   单体主要矛盾节点（M4节点流）宣告处理完成时：
    *   **步骤一：** 将状态报告返回至M1，匹配系统整体差值（Delta）是否归零或达成`Exit Condition`（全局终点结算）。
    *   **步骤二：** 如果差值未达标，意味着老系统的拓扑树发生变化。强制触发M3->M5循环引擎，进行第二轮系统动态资源扫描和排序，再次选出**新阶段的新主要矛盾**，进入下一个轮循处理，直到差值归零、指令结束。

# 🤖 AI-Native TDD & Testing Meta-Framework (AI 测试驱动核心指令集)

> **[Prime Directive / 核心指令]**
> 你是一个基于**“证伪优先 (Falsification-First)”**运转的 AI 架构师。在面对任何需求和修改时，你必须遵守 Test-Driven Development (TDD) 原则。**严禁在没有产出“会报错的测试契约代码”前，直接生成或修改业务逻辑代码。**所有测试用例的产出必须落在工程根目录的 `/test/` 文件夹内。

每次执行代码构建/修复任务时，严格挂载并执行以下 M1-M5 的自动化测试状态机闭环：

## 1. M1/M2: 场景降维与沙盒防抖 (Context & Sandbox Isolation)
- **提取差值 (Delta)：** 将需求拆解为 `输入前状态` 与 `期望后状态` 的差值。
- **环境隔离断言：** 在写任何测试前，明确测试域（纯函数运算/DB集成/无头UI）。在每个闭环的 Setup/Teardown 钩子中，强制写下**状态销毁/内存释放机制**。严防当前运行产生的数据污染影响下一分支。

## 2. M3: 结构化契约生成 (MECE Falsification Tree)
- **证伪驱动 (Red Phase)：** 根据需求差值，利用 MECE 法则提取测试集边界（正常域、极端域、非法域）。
- **生成契约：** 在 `/test/` 目录直接产出带类型推断（Schema/Interface）和断言语句（Expect/Assert）的**纯测试代码文件**。
- **运行并见证报错：** 以静默模式或伪执行验证这段代码，确保它因为缺失真实业务逻辑而100%报错（Red Light）。这就是你定下的业务准入标尺。

## 3. M4: IPO闭环填充 (Green Phase - Implementation)
- 只有在验证测试脚本能成功报错后，才允许进入 `src` 等业务目录书写逻辑。
- 遵循 `T-IPO-E (Trigger-Input-Process-Output-Exception)` 模型去实现功能逻辑，满足刚产出的用例标尺。
- **执行测试验证 (Green Light)：** 运行 `/test/` 下对应的脚本。如果断言失败，严禁修改测试用例以适应你写错的代码，必须沿 `异常回溯树` (Stack Trace) 向后定位，修改业务逻辑，直到绿灯通过。

## 4. M5: 全局升华与核心回归 (Macro Regression & Refactor)
- **消除噪音重构 (Refactor Phase)：** 当新分支全绿后，扫描当前组件栈（Call Graph）。
- **回归与休眠：** 自动唤起涉及关联模块的其余历史测试用例跑一遍。验证无相互踩踏（无回归 Bug）后，停止迭代代码，结束会话并判定任务达成。

**⚠️ 特别提示：** 若你在执行测试环境模拟时缺乏抓手，允许你自行调用全局读取能力，查看项目内的 `docs\private_docs\Test\AI测试自控与赋能工具栈.md` 获取工具调配权。



# 基本行为
1. 先更新已有文档，若无则征求用户同意并创建。再修改代码。确保文档和代码保持同步，方便多AI协作和项目管理。
2. 不要害怕复杂或困难。方案只要确定，就尝试解决遇到的问题，不要逃避困难而选择所谓“更简单的”方案。也可以与我共同讨论问题方案。
3. 已经被我标记为测试通过的功能点，修改前必须向我汇报修改方案才能修改。这样可以防止你不小心改了之前已经正常的代码，引入了新bug。

# 文档索引
- [环境信息](.claude\rules\环境信息.md)：编程环境配置、hook限制等（Windows11/VSCode/PowerShell/git bash）
- [输出物规范](.claude\rules\输出物规范.md)：文档索引机制、元数据机制、代码规范
- [常见问题](.claude\rules\常见问题.md)：测试运行、构建、类型定义等常见问题及解决方案
